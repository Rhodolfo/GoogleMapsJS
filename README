Right now this just returns all stablishments 
in a nearbyPlace search query to Google Maps 
within a circle of a certain radius centered around a certain coordinate.

Your Google API key must be set in http.js
Coords are set in index.js (for now, I'll change this).

Run once dependencies have been installed
$ node index.js
You'll need to wait at least 5 seconds, 
calls to the Google Maps API need to be delayed

I really should implement exponential backoff
https://developers.google.com/analytics/devguides/reporting/core/v3/errors#backoff

Disk covering problem (this is relevant)
https://en.wikipedia.org/wiki/Disk_covering_problem

Docs on google places API
https://developers.google.com/places/web-service/search

DEPENDENCIES

This one's needed
$ npm install request
